# Contributing

First, install `uv` if you don't already have it.

```bash
pip install uv
```

To launch the notebook environment, use:

```bash
uv sync
uv run marimo edit SfM.py
```

This should launch the Jupyter notebook interface in the browser.
